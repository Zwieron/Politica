using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PollPhase : GamePhase
{
    // Start is called before the first frame update
    void Start()
    {
        getGame();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
