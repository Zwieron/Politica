using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum GamePhases
{
    // Start is called before the first frame update
    BiddingPhase, ActionPhase, PollPhase
}
